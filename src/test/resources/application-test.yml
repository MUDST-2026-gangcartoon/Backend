spring:
  datasource:
    url: jdbc:h2:mem:eventhub-test
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

  h2:
    console:
      enabled: false